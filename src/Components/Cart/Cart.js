import React from 'react';

const Cart = (props) => {
    console.log(props.cart);
    return (
        <div>
            <h4>Cart</h4>
            <p></p>
        </div>
    );
};

export default Cart;